import React from 'react';
import cl from './select/Loader.module.css'
const Loader = () => {
    return (
        <div className={cl.loader}>
            
        </div>
    );
};

export default Loader;